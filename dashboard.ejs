<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Dashboard</title>
    
</head>
<body class="bg-light">

    <div class="container mt-5">
        <div class="card shadow-lg p-4">
            <h1 class="text-center">Welcome, <%= user.name %></h1>
            <p class="text-center text-muted">Email: <%= user.email %></p>
            <p class="text-center text-muted">Role: <span class="badge bg-primary"><%= user.role %></span></p>
            
            <!-- Display current profile picture -->
            <div class="text-center my-4">
                <h4>Profile Picture</h4>
                <img src="/images/<%= user.profilePicture %>" alt="Profile Picture" class="rounded-circle border border-secondary" width="150" height="150">
            </div>

            <!-- Form to update profile picture -->
            <div class="mt-4">
                <h5>Update Profile Picture</h5>
                <form action="/update-profile-pic" method="POST" enctype="multipart/form-data" class="d-flex align-items-center gap-3">
                    <input type="file" name="profilePicture" id="profilePicture" class="form-control" required>
                    <button type="submit" class="btn btn-success">Upload</button>
                </form>
            </div>
            <div class="mt-4 text-center">
                <a href="/logout" class="btn btn-danger">Logout</a>
                <a href="/chat" class="btn btn-primary">Chat</a> <!-- Chat button -->
            </div>
        </div>
    </div>
  
   

</body>
</html>
